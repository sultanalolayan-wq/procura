{
  "name": "ares-swarm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ARES — Autonomous Revenue & Enforcement Swarm (PAPER mode only).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/",
    "start": "node dist/src/index.js",
    "dev": "npm run build && node dist/src/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6",
    "@types/node": "^22"
  }
}
